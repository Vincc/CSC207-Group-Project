package use_case.makeEvent;

import junit.framework.TestCase;

public class makeEventOutputDataTest extends TestCase
{
    makeEventOutputData makeEventOutputData = new makeEventOutputData();
    public void test(){
        assertTrue(makeEventOutputData instanceof makeEventOutputData);
    }
}